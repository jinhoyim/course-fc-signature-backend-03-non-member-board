spring:
  datasource:
    url: jdbc:mysql://localhost:3306/simple_board_test?useSSL=false&useUnicode=true&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: testdbuser
    password: ${LOCAL_DEVDB_PASSWORD}
